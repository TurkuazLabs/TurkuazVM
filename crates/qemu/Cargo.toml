# 📄 Dosya Yolu: /turkuazvm/crates/qemu/Cargo.toml
# 📌 Amac: QEMU infrastructure adapter crate manifestini tanimlar
# 📌 Modul - TOML
# Version: 0.28.0
# Aciklama: QEMU discovery, QMP runtime, guest boot, GPU launch ve prepared network plan rendering adapterlerini barindirir
# Bagimli Oldugu Katman: Tool

[package]
name = "turkuazvm-qemu"
version.workspace = true
edition.workspace = true
rust-version.workspace = true
license.workspace = true

[dependencies]
serde.workspace = true
serde_json.workspace = true
turkuazvm-core = { path = "../core" }
turkuazvm-gpu = { path = "../gpu" }

[lints]
workspace = true
